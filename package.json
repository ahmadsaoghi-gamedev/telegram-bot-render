{
  "name": "telegram-bot-webapp",
  "version": "1.0.0",
  "description": "Telegram bot with webapp for streaming platform",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed'",
    "start": "echo 'Use vercel dev for local development'"
  },
  "engines": {
    "node": "18.x"
  }
}
